package org.babyfish.kimmer.sql.runtime

import org.babyfish.kimmer.sql.ast.Selection

private fun mapResult(selections: Selection<*>, reader: (Int) -> Any?): Any? {
    return null
}

private fun mapSelection(selection: Selection<*>, reader: (Int) -> Any?) {

}